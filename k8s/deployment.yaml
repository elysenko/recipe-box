apiVersion: apps/v1
kind: Deployment
metadata:
  name: recipe-box-staging
  namespace: colossus-recipe-box-staging
  labels:
    app: recipe-box-staging
    managed-by: colossus
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: recipe-box-staging
  template:
    metadata:
      labels:
        app: recipe-box-staging
    spec:
      hostAliases:
        - ip: "192.168.50.250"
          hostnames:
            - "ubuntu.desmana-truck.ts.net"
      containers:
        - name: app
          image: ubuntu:30500/recipe-box-staging:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: app-secrets
                optional: true
