apiVersion: v1
kind: Service
metadata:
  name: recipe-box-staging
  namespace: colossus-recipe-box-staging
  labels:
    app: recipe-box-staging
    managed-by: colossus
spec:
  type: ClusterIP
  selector:
    app: recipe-box-staging
  ports:
    - port: 80
      targetPort: 80
